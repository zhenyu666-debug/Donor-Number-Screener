rdkit-pypi>=2022.9.5
scikit-learn>=1.3.0
xgboost>=2.0.0
optuna>=3.0.0
lightgbm>=4.0.0
catboost>=1.2.0
pandas>=2.0.0
numpy>=1.24.0
numpy<2
matplotlib>=3.7.0
seaborn>=0.12.0
shap>=0.42.0
tqdm>=4.65.0
joblib>=1.3.0
markdown>=3.0
pdfkit>=1.0.0
fpdf2>=2.7.0          # v3: pure-Python PDF fallback
fastapi>=0.100.0      # v3: REST API server
uvicorn>=0.23.0       # v3: ASGI server
pydantic>=2.0.0       # v3: input validation
bs4>=0.0.1            # v3: HTML parsing for fpdf2 backend
scipy>=1.10.0         # v3: Pearson/Spearman for external validation
ruff>=0.1.0           # v3: code-quality linter
pyyaml>=6.0            # v2.1: PBP utils_pb / fetch_sse YAML I/O
# Optional: better PDF rendering
# weasyprint>=60.0  # requires cairo/pango system libraries

# ---------------------------------------------------------------------------
# v2.1 — PBP (Particle-Bayes-Physics) layer
# ---------------------------------------------------------------------------
# Optional heavy ML potentials — install with --no-deps if a wheel is missing
# on Python 3.11 / numpy<2. The PBP physics code falls back to plain LJ + Coulomb
# when ase / mace-torch / chgnet are unavailable, so the FastAPI endpoints and
# pytest suite still work.
ase>=3.23            # PBP ML-AIMD (LJCalculator fallback when ase missing)
mace-torch>=0.3.10   # PBP ML-AIMD foundation model (optional, fallback LJ)
chgnet>=0.3.0        # PBP ML-AIMD foundation model (optional, fallback LJ)
pymatgen>=2024.9     # PBP ML-AIMD structure (optional, fallback LJ)
torch>=2.0           # PBP ML-AIMD backend (CPU-only is fine)

# ---------------------------------------------------------------------------
# v5 — Interactive demo
# ---------------------------------------------------------------------------
streamlit>=1.28       # v5: interactive demo UI (runs alongside the FastAPI server)
requests>=2.31        # v5: API client for streamlit
