test: add compute_drift_report and calibrate_top20 coverage; fix dead code

- test_v5.py: add test_p47_compute_drift_report (validates report
  structure, severity assignment, and empty-data handling with seed).
  add test_p46_calibrate_top20_schema (patches DATA_DIR to tmp_path,
  verifies dn_iso_calibrated and dn_platt_calibrated columns are written).
  add import pandas as pd to support new tests.

- p46_calibration.py: remove dead pd.read_csv(DATA_DIR / "descriptors_v2.csv")
  call (read without using, replaced by plain labels read that is actually used).
